# ==========================================================
# OpenClaw Docker Multi-Service Environment Configuration
# ==========================================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env

# --- Authentication (REQUIRED) ---
OPENCLAW_GATEWAY_TOKEN=changeme

# --- Gateway ---
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_OAUTH_PORT=1455

# --- VNC Configuration ---
VNC_PORT=5901
NO_VNC_PORT=6080
VNC_PW=openclaw
VNC_RESOLUTION=1280x1024

# --- Distributed Deployment ---
# Set OPENCLAW_GATEWAY_ADDR on node hosts to the gateway's IP/hostname.
# Leave empty for single-host (all-in-one) deployment.
# OPENCLAW_GATEWAY_ADDR=192.168.1.10

# Node display names (used for pairing and configuration)
# OPENCLAW_SYSTEM_NODE_NAME=system-node
# OPENCLAW_VNC_NODE_NAME=vnc-node

# --- Advanced (rarely changed) ---
NODE_ENV=production
OPENCLAW_HOME=/var/lib/openclaw
OPENCLAW_STATE_DIR=/var/lib/openclaw/state
OPENCLAW_CONFIG_PATH=/etc/openclaw/config.json
